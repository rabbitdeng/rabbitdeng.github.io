# Basic Configuration
title: My Blog
description: A simple Jekyll blog hosted on GitHub Pages
author: 邓果垚
email: 741533684@qq.com

baseurl: "" # the subpath of your site, e.g. /blog
url: "https://rabbitdeng.github.io" # the base hostname & protocol for your site
repository: rabbitdeng/rabbitdeng.github.io # GitHub repository in format username/repo-name

# Build settings
theme: minima
plugins:
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate
markdown: kramdown
highlighter: rouge
permalink: /:year/:month/:day/:title/

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/

# Pagination
paginate: 5
paginate_path: "/page:num/"